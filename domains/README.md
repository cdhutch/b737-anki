# Domains

This repo is organized by `domains/<domain>/...` so we can support multiple aircraft / knowledge domains
(e.g., b737, a320, general-aviation, ukrainian, etc.) with shared tooling under `/config` and `/domains/_template`.
